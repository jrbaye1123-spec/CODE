# Computations

Original Python computation code. Every file here is a runnable computation, experiment, probe, or training script.

| Directory | What it does |
|---|---|
| `baby-adam/` | Minimal Adam-from-scratch training loop + GGUF export |
| `kitadel/` | Kitadel computation |
| `logdet-probe/` | LogDet manifold-conditioning probe (numpy + torch backends) |
| `moe-training/` | MoE expert training, spectral regularizer, fixed-point & spectral probes |
| `nobody-ledger/` | "Nobody ledger" — ledger + spectrum computation |
| `organ-fleet/` | Organ-fleet: death-clock math, haptic transducer, logit/hue/witness probes |
| `vault-rag/` | RAG over the vault — indexer, retriever, MoE retriever, trainer, server |
| `llama-probe-runtime/` | Reference extraction + validation for probe runtime |
| `experiments/` | Phase diagram, closed-FLRW Berry curvature, Schatten convergence runs |
